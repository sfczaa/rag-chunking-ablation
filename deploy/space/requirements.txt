# torch is intentionally unpinned/omitted: the ZeroGPU image ships a supported
# CUDA build, and installing our own would shadow it.
spaces
sentence-transformers>=6.0.1
faiss-cpu>=1.7
huggingface_hub>=0.20
numpy>=1.24
